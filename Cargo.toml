[package]
name = "periodic"
version = "0.1.0"
edition = "2024"
description = "A user-space recurring job scheduler for developer machines, devboxes, and lightweight infrastructure."
repository = "https://github.com/dbtlr/periodic"
license = "MIT OR Apache-2.0"

[dependencies]
